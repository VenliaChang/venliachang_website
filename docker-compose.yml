services:
  web:
    build: .
    ports:
      - "8080:80"
    # Dev: mount local files so changes reflect without rebuild
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
      - ./css:/usr/share/nginx/html/css:ro
      - ./js:/usr/share/nginx/html/js:ro
      - ./assets:/usr/share/nginx/html/assets:ro
      - ./articles:/usr/share/nginx/html/articles:ro

  # --- Future Python backend ---
  # api:
  #   build: ./api
  #   ports:
  #     - "8000:8000"
  #   depends_on:
  #     - db
  #
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: venlia_db
  #     POSTGRES_USER: venlia
  #     POSTGRES_PASSWORD: changeme
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #
  # volumes:
  #   pgdata:
